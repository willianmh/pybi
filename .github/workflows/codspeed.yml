name: CodSpeed

on:
  push:
    branches:
      - "main"
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  simulation-benchmarks:
    name: Run simulation benchmarks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Set up Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --dev

      - name: Run CPU-focused benchmarks (simulation)
        uses: CodSpeedHQ/action@v4
        with:
          mode: simulation
          run: uv run pytest tests/ --codspeed -m "not io_benchmark"

  walltime-io-benchmarks:
    name: Run I/O benchmarks (walltime)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Set up Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --dev

      - name: Run filesystem I/O benchmarks (walltime)
        uses: CodSpeedHQ/action@v4
        with:
          mode: walltime
          run: uv run pytest tests/ --codspeed -m "io_benchmark"
